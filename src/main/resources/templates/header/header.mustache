<!doctype html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="icon" href="/images/favicon.ico">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Gowun+Dodum&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="css/header_style.css">
    <link rel="stylesheet" href="//code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
</head>
<script src='https://code.jquery.com/jquery-3.3.1.min.js'></script>
<script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
<body>
<header>
    <nav>
        <ul>
            <li id="li_linktree"><a href="/">LINK TREE</a></li>
            <li id="li_search_form">
                <form action="/user_search" method="get" id="search_form">
                    <div id="div_search">
                        <input class="search-input" type="text" name="username" id="search"
                               placeholder="유저를 검색하세요"/>
                        <button class="search-btn" type="submit">S</button>
                    </div>
                </form>
            </li>
            {{#user.username}}
                <li class="li_text">
                    <span class="a_text">{{user.username}}님</span>
                    <button type="button" onclick="location.href='iduser_link'" class="user_btn">내 링크보기</button>
                    {{#user.name}}
                        <button type="button" onclick="location.href='update'" class="user_btn" style="margin-left: 5px">정보수정</button>
                    {{/user.name}}
                    {{#user.name}}
                        <form action="/delete" method="post" class="li_btn_form">
                            <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}"/>
                            <button type="submit" class="user_btn">탈퇴하기</button>
                        </form>
                    {{/user.name}}

                    <form action="/logout" method="post" class="li_btn_form">
                        <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}"/>
                        <button type="submit" class="user_btn">로그아웃</button>
                    </form>
                </li>
            {{/user.username}}
            {{^user.username}}
                <li class="li_text"><span class="a_text">로그인해주세요</span>
                    <button type="button" onclick="location.href='login'" class="user_btn">로그인</button>
                    <button type="button" onclick="location.href='signup'" class="user_btn" style="margin-left: 5px">회원가입</button>
                </li>
            {{/user.username}}
        </ul>
    </nav>
</header>
</body>
</html>